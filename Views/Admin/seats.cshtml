@model IEnumerable<OnlineMovie.DM.seat>

@{
    ViewBag.Title = "seats";
    Layout = "~/Views/Shared/_AdminLayoutPage.cshtml";
}

<h2>seats</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.seat_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.seat_num)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cat_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.schedule_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.price)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.seat_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.seat_num)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cat_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.schedule_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.price)
        </td>
        <td>            
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
